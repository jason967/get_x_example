import 'package:get/get.dart';
import 'package:get_x_example/wau_reservation/calender/utils.dart';

class CalenderController extends GetxController {
  var selectedDay = kToday.obs;

  // indexChange(int idx) => currentIdx(idx);
}
